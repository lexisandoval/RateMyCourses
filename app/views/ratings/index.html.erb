<div class="container centered">
  <h1 class="brand head">Recent Course Ratings</h1>

  <% @ratings.each do |rating| %>
    <div>
      <h2><%= rating.course.title_with_number %></h2>
      
      <h3><% rating.stars.times do  %>
        <%= "⭑" %>
      <% end %></h3>
      <%= rating.content %>
    </div>
  <% end %>

</div>