<div class="container mx-auto my-auto">
      <h1 class="bold brand subhead">Rating from <%= @rating.created_at.strftime("%A, %d %b %Y") %></h1><br>
      <h5 class="classic">You gave <span class="plain"><%= @rating.course.full_title %></span> <%= @rating.stars %> stars.</h5>
      <h5><% @rating.stars.times do  %>
            <%= "⭑" %>
      <% end %></h5><br>
      <p>"<%= @rating.content %>"</p>

      <br>

      <p><a class="black no_underline main brand" href="/users/<%= @rating.user_id %>/ratings/<%= @rating.id %>/edit">Edit</a></p>
      <p>-------------</p>
      <p class="black no_underline main brand"><%= link_to "Delete", user_rating_path(@rating.user, @rating), :method => "delete", :class => "destroy", :style=>'color:black;text-decoration:none;' %><p>
      <p>-------------</p>
      <p><a class="black no_underline main brand" href="/users/<%= @rating.user_id %>">Go Back</a></p>
</div>